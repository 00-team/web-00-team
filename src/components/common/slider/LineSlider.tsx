import React from 'react'

interface LineSliderProps {}

const defaultProps: LineSliderProps = {}

const LineSlider = () => {
    return <div>Line</div>
}

LineSlider.defaultProps = defaultProps

export { LineSlider }
